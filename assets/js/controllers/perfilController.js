const perfilController = {

    init() {
        // Ainda sem lógica: a página é estática por agora.
        // Quando Favoritos/Preferências forem implementados,
        // este controller deve chamar services/favoritosService.js
        // (já existe, vazio) e renderizar via components/ dedicados.
    }

};